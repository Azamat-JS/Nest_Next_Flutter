'use client'


const GroupDetailsComponent = ({ productId }: { productId: string }) => {
    console.log(productId)
    return (
        <div>GroupDetailsComponent {productId}</div>
    )
}

export default GroupDetailsComponent